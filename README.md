# E-Commerce Order Microservices


## Premium E-Commerce Frontend

The project includes a responsive GadgetCart phone-order frontend with a premium gradient card and phone icon. The frontend is served on port 3000 and sends order requests through Nginx to the Order Service.

Start the project from this folder:

```bash
docker compose up --build
```

Open http://localhost:3000.
